# ─── Added by feature/phase2-foundation ────────────────────────────
# llm_client
litellm>=1.50,<2.0
redis>=5.0,<6.0           # for LLM cache + progress pub/sub

# chunking
tiktoken>=0.7,<1.0
nltk>=3.8,<4.0

# embedding
sentence-transformers>=3.0,<4.0
langdetect>=1.0.9,<2.0

# Test-only (pytest-asyncio, fakeredis used by progress tests)
fakeredis>=2.23,<3.0
