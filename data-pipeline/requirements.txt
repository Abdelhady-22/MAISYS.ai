# MAISYS data-pipeline dependencies.
# Pins are lower-bounds on the majors validated during development.

# Cloud upload bridge (cloud/upload_local_to_gcs.py)
google-cloud-storage>=2.16,<4
pydantic>=2.7,<3
structlog>=24.1
tqdm>=4.66
PyYAML>=6.0

# MTSamples Kaggle downloader (downloaders/download_mtsamples_kaggle.py)
kaggle>=1.6
# HuggingFace downloader (downloaders/download_huggingface_medical.py)
datasets>=2.19
huggingface_hub>=0.23
google-cloud-secret-manager>=2.20

# Cross-cloud sync (cloud/sync_gcs_to_azure.py)
azure-storage-blob>=12.20
azure-identity>=1.16

# Cross-cloud sync (cloud/sync_gcs_to_aws.py)
boto3>=1.34
botocore>=1.34

# Mayo Clinic scrapers (scrapers/mayo_clinic/) — Playwright + stealth + bs4.
# NOTE: the Phase B VM must also run `playwright install chromium` (+ install-deps).
playwright>=1.44
playwright-stealth>=1.0.6
beautifulsoup4>=4.12

# Tooling (lint / format / type-check / test)
ruff>=0.5
black>=24.4
mypy>=1.10
types-PyYAML>=6.0
pytest>=8.2